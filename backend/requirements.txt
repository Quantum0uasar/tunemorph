# TuneMorph backend dependencies
# Install: pip install -r backend/requirements.txt

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Audio processing (core — required for note detection)
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4
scipy==1.13.1

# Pydantic (data validation)
pydantic==2.10.3

# Optional: faster audio I/O
audioread==3.0.1
